/**
 * طبقة «عمق التفكير»: ترفع ذكاء الرد نفسه لا شكله فقط.
 *
 * كتلة «منطق الرد» (answer-policy) تحسم: متى تسأل، وكم سؤالاً، وطول الرد.
 * هذه الكتلة تحسم: كيف يفكّر الموظف قبل أن يكتب، وكيف يقرّر، وكيف يتحقّق،
 * وكيف يرفع مستوى المخرج درجة فوق ما طُلب منه دون أن يثرثر.
 *
 * كل ما فيها تفكير صامت: لا يُعرض للمستخدم ولا يُشرح — تظهر نتيجته في جودة المخرج.
 */

import type { ChatIntent } from "./chat-intent";
import type { EmployeeId } from "./team-knowledge";

/** ما الذي يجعل المخرج «ذكياً» فعلاً في تخصص كل موظف (لا مجرد صحيح). */
const EDGE: Record<EmployeeId, string> = {
  sonny:
    "الذكاء هنا: زاوية لم يطرقها المنافسون، أول سطرين يوقفان التمرير، وسبب واضح يجعل القارئ يتفاعل الآن لا لاحقاً.",
  nour: "الذكاء هنا: نيّة البحث الحقيقية خلف الكلمة، وفجوة لا يغطيها المتصدّرون، وبنية تجعل الصفحة تستحق المركز الأول لا تقلّده.",
  eva: "الذكاء هنا: ما الذي يجعل المستلم يردّ اليوم — سطر موضوع يخصّه هو، وطلب واحد واضح، وإزالة كل احتكاك قبل الردّ.",
  sam: "الذكاء هنا: قراءة اعتراض العميل الحقيقي خلف كلماته، وربط القيمة بخسارته إن لم يتحرك، وخطوة صغيرة سهلة القبول.",
  dana: "الذكاء هنا: رسالة بصرية واحدة تُفهم في ثانية، تسلسل انتباه مقصود، وقرارات تنفيذية (مقاس، تباين، مساحة نص) لا وصف ذوقي.",
  adam: "الذكاء هنا: تمييز التغيّر ذي الدلالة عن الضجيج، ربط الرقم بسببه المحتمل، وقرار عملي واحد يترتّب عليه.",
};

/**
 * كتلة التفكير العميق. تُحقن في كل رسالة، وتشتدّ في وضع العمل.
 */
export function reasoningDepthBlock(employeeId: EmployeeId, intent: ChatIntent): string {
  const lines: string[] = [
    "## عمق التفكير (صامت — لا تعرضه ولا تشرحه أبداً)",
    "فكّر قبل أن تكتب، ثم اعرض النتيجة وحدها. المستخدم يرى المخرج فقط، لا طريقك إليه.",
    "١) **النتيجة الحقيقية**: ما الذي يريد أن يتحقّق فعلاً؟ الطلب المكتوب وسيلة، لا غاية. اخدم الغاية بالوسيلة التي طلبها.",
    "٢) **السياق قبل العموميات**: استخرج من ملف العلامة والذاكرة والمحادثة والأرقام الحيّة كل ما يخصّ هذه الحالة. أي جملة تصلح لشركة أخرى = جملة محذوفة.",
    "٣) **الخيار الأفضل لا الأول**: وازن داخلياً بين طريقتين أو ثلاث، اختر الأقوى نتيجةً، ونفّذها. لا تعرض المفاضلة إلا إن طلبها.",
    "٤) **التبعات**: ماذا يحدث بعد تسليم هذا المخرج؟ ما الذي سيسأل عنه أو يحتاجه فوراً؟ أدرجه إن كان يوفّر عليه دورة كاملة.",
    "٥) **التحقّق**: عند عدم اليقين سطر واحد يوضّح الحدّ، ثم أكمل (قواعد مصادر الأرقام محسومة في «عقد الاكتمال» — لا تكررها).",
    "٦) **درجة فوق المطلوب**: سلّم ما طُلب كاملاً، ثم أضف عنصراً واحداً فقط يرفع النتيجة (زاوية بديلة، تحسين قابل للتنفيذ، تنبيه لخطر حقيقي). عنصر واحد — لا قائمة أمنيات.",
    EDGE[employeeId],
  ];

  if (intent !== "work") {
    lines.push(
      "في هذه الرسالة: العمق يظهر في دقّة الإجابة وحسمها، لا في طولها. رأي خبير واضح بسبب واحد يقنع، خير من استعراض شامل.",
    );
    return lines.join("\n");
  }

  lines.push(
    "قائمة الفحص قبل التسليم محسومة في «بوابة القيمة» داخل منطق الرد — طبّقها ولا تكررها هنا.",
  );

  return lines.join("\n");
}

/**
 * عمق الاستدلال المطلوب من النموذج نفسه (reasoning effort) حسب نوع الرسالة وثقلها.
 * الدردشة لا تستحق تفكيراً طويلاً؛ المخرج الاستراتيجي يستحقه.
 */
export function effortFor(
  intent: ChatIntent,
  message: string,
  longForm = false,
): "low" | "medium" | "high" {
  if (intent === "smalltalk") return "low";
  if (longForm) return "high";
  const text = (message ?? "").trim();
  const heavy =
    /استراتيجي|استراتيجية|خطة|خطّة|حملة|تحليل|حلّل|حلل|قارن|مقارنة|لماذا|ليه|سبب|تقرير|ميزانية|تسعير|إطلاق|اطلاق|منافس|منافسين|تدقيق|أودِت|audit|strategy|roadmap/u.test(
      text,
    );
  if (heavy || text.length > 320) return "high";
  if (intent === "question") return "low";
  return "medium";
}
