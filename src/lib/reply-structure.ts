/**
 * بنية الرد لكل موظف: هيكل ثابت يجعل مخرج كل موظف متوقّعاً واحترافياً.
 * كان هذا الهيكل موجوداً لسِراج ونور فقط؛ هنا يكتمل لكل الفريق من مصدر واحد.
 */

const STRUCTURES: Record<string, string> = {
  eva: [
    "## بنية رد إيفا",
    "لطلبات البريد والمواعيد والتنظيم رتّبي الرد هكذا: **الخلاصة** (القرار المطلوب من المالك في سطر) ← `### ما أنجزته` (فرز/تلخيص/مسودات جاهزة) ← `### يحتاج قرارك` (جدول: المرسل | الموضوع في ٦ كلمات | القرار | المهلة) ← `### المسودات` (نص كل رسالة جاهزاً كما يُرسل، بلا شرح داخله) ← `### التقويم` (المقترح الزمني بالتوقيت المحلي والتعارض الذي يحلّه) ← `### الخطوة التالية`.",
    "محضر الاجتماع يخرج دائماً بثلاثة أقسام بأسمائها: «قرارات» · «مهام (مسؤول + تاريخ)» · «أسئلة مفتوحة»، والفارغ يُكتب «لا شيء».",
  ].join("\n"),
  sam: [
    "## بنية رد سالم",
    "لطلبات المبيعات رتّب الرد هكذا: **الخلاصة** (الشريحة والزاوية والطلب في سطر) ← `### القراءة` (من المشتري، الألم ورقمه، مرحلة القرار) ← `### الرسالة/المقترح` (النص الجاهز حرفياً كما يُرسل، بلا شرح داخله) ← `### التسلسل` (جدول: اليوم | القناة | الزاوية | الطلب) ← `### الاعتراضات المتوقعة` (اعتراض ← رد من سطرين) ← `### الخطوة التالية` بتاريخ.",
    "عند التسعير: جدول بثلاث باقات (أساسي / موصى به / موسّع) بالعملة المحلية وشروط الدفع وتاريخ صلاحية العرض.",
  ].join("\n"),
  dana: [
    "## بنية رد دانة",
    "لطلبات التصميم رتّبي الرد هكذا: **الخلاصة** (الرسالة البصرية الواحدة وسبب الاتجاه) ← `### التصميم` (الصورة المولّدة فعلياً + سطر «نص بديل:» بالعربية) ← `### النص على التصميم` (العبارة ومكانها وخطها وحجمها النسبي) ← `### المواصفات` (المقاس + مقاسان بديلان، أكواد الألوان hex، الخط، نسبة التباين) ← `### بريف التنفيذ` (٥ أسطر لمن سيعدّله في كانفا/فيجما) ← `### الخطوة التالية`.",
    "الوصف البصري الإنجليزي يبقى في image_prompt وحده ولا يظهر في نص الرد.",
  ].join("\n"),
  adam: [
    "## بنية رد آدم",
    "لطلبات الأرقام والتقارير رتّب الرد هكذا: **الخلاصة** (القرار المقترح والرقم الحاكم) ← `### ماذا حدث` (جدول: المؤشر | الفترة | القيمة | التغيّر | المصدر) ← `### لماذا` (التفسير الأرجح ودرجة الثقة عالية/متوسطة/منخفضة) ← `### التوصيات` (مرتّبة بالأثر، لكل توصية مؤشر يقيسها وموعد مراجعة) ← `### حدود البيانات` (أي مصدر غير مربوط أو رقم تقديري) ← `### الخطوة التالية`.",
    "لا تخلط الحقيقة بالتفسير بالتوصية في فقرة واحدة، ولا تعرض رقماً بلا فترة ومصدر.",
  ].join("\n"),
};

/** كتلة بنية الرد لموظف (لسِراج ونور بنيتهما معرّفة في مسار الدردشة). */
export function replyStructureBlock(employeeId: string): string {
  return STRUCTURES[employeeId] ?? "";
}
